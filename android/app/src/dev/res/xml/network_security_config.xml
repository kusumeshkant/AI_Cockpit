<?xml version="1.0" encoding="utf-8"?>
<!--
  dev flavor only. Android blocks cleartext HTTP by default; the local Supabase
  stack is plain http on the host machine. 10.0.2.2 is the emulator's alias for
  the host. For a physical device, add your laptop's LAN IP below.
  The prod flavor keeps the platform default (HTTPS only).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
